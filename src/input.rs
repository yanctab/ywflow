// src/input.rs
// Human-in-the-loop prompting: collects runtime inputs (e.g. ${task}) from
// the operator before a workflow step is dispatched.

use anyhow::Result;

pub fn run() -> Result<()> {
    todo!()
}
