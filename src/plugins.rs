// src/plugins.rs
// Plugin/skill loader — discovers and registers per-step Claude Code skills
// and injects them into the session environment.

use anyhow::Result;

pub fn run() -> Result<()> {
    todo!()
}
